<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="quemen_view_pos_session_form" model="ir.ui.view">
        <field name="name">quemen.pos.session.form.view</field>
        <field name="model">pos.session</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_session_form"/>
        <field name="arch" type="xml">
            <!-- <button name="action_pos_session_closing_control" position="after">
                <button name="generar_factura_global_sesion" type="object" string="Generar factura global" states="closed" class="oe_highlight"/>
            </button>       -->
        <group>
            <field name="pendiente_facturar"/>
            <field name="factura_global_id"/>
              <!-- <field name="cash_register_total_entry_encoding"/> -->
              <field name="saldo_apartura"/>
              <field name="total_efectivo_caja"/>
              <field name="pagos_efectivo"/>
              <field name="retiros_efectivo"/>
          </group>
            <!-- <xpath expr="//page[1]" position="after">
                <page name="retiros" string="Retiros">
                    <group>
                        <field name="retiros_ids"/>
                    </group>
                </page>
            </xpath> -->

            <!-- <group>
                <field name="retiros_ids"/>
            </group> -->
        </field>
    </record>

</odoo>
